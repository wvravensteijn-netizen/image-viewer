{
  "configurations": [
  {
    "type": "lldb",
    "request": "launch",
    "name": "Launch",
    "program": "${workspaceFolder}/build/debug/bin/image-viewer.exe",
    "args": [],
    "cwd": "${workspaceFolder}"
  }
  ]
}